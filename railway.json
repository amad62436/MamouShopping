{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --optimize-autoloader --no-dev"
    },
    "deploy": {
        "startCommand": "php artisan optimize:clear && php artisan storage:link && php artisan serve --port=$PORT --host=0.0.0.0",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
}
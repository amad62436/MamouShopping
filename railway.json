{
    "build": {
        "builder": "nixpacks",
        "buildFlags": "--add pkgs.php83Extensions.redis"
    },
    "deploy": {
        "startCommand": "php artisan serve --port=$PORT --host=0.0.0.0",
        "restartPolicyType": "ON_FAILURE"
    }
}